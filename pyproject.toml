[project]
name = "polygon-iter"
version = "0.1.0"
description = "Iterative polygon visualization"
authors = [{name = "David Danka"}]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[tool.setuptools.packages.find]
where = ["src"]  # <-- this is the only addition